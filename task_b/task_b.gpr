with "config/task_b_config.gpr";
project Task_B is

   for Source_Dirs use ("src/", "config/");
   for Object_Dir use "obj/" & Task_B_Config.Build_Profile;
   for Create_Missing_Dirs use "True";
   for Exec_Dir use "bin";
   for Main use ("task_b.adb", "ravenscar_demo.adb");

   package Compiler is
      for Default_Switches ("Ada") use Task_B_Config.Ada_Compiler_Switches;
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es"); --  Symbolic traceback
   end Binder;

   package Install is
      for Artifacts (".") use ("share");
   end Install;

end Task_B;
